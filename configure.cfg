APP=vhs
APP_REQUIRES="ffmpeg"
BINDIR=bin/vhs
DOCDIR=docs
COMPLDIR=files/vhs_compl.bash
MAN1DIR=man
PRIOR[0]="cp bin/vhs.sh bin/vhs"
PRIOR[1]="which tuirc > prior.text"
PRIOR[2]='[ -f "$(<prior.text" ] && ( git clone https://github.com/sri-arjuna/tui tui || echo "Please install \"git\" and try again!")'
PRIOR[2]="[ -d ./tui ] && cd tui && ./configure --prefix=$prefix && ./make-install"
REMOVE[0]="rm bin/vhs prior.txt"
BUGS=erat.simon@gmail.com
ISSUE="https://github.com/sri-arjuna/vhs/issues"
